#include <iostream>
using namespace std; 

string toString(char* name)
{
	
	/* char name[] = " hello "; */
	string str(a);
	/*
	char ch;
	int i = 0;
	while ( ch != '\0' ){
	ch = name[i++];	
	a += to_string(ch);
	}
	* cout << a <<endl; */
	return a;
}

int main(int argc, char *argv[])
{
	char name[] = " hello ";
	cout << toString( name ) <<endl;
	return 0;
}
